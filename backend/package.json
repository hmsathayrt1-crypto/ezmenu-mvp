{
  "name": "ezmenu-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute ezmenu-db --file=./src/schema.sql",
    "db:seed": "wrangler d1 execute ezmenu-db --file=./src/seed.sql"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250514.0",
    "wrangler": "^4.18.0",
    "typescript": "^5.8.0"
  }
}
